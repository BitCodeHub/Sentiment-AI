# Production Environment Variables for Frontend

# Backend API URL (optional - defaults to production URL)
# VITE_BACKEND_URL=https://sentiment-review-backend.onrender.com

# Apple API endpoint (optional - defaults to production URL)
# VITE_APPLE_API_ENDPOINT=https://sentiment-review-backend.onrender.com/api/apple-reviews

# Note: Reddit credentials should be set in the backend environment, not frontend