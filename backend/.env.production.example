# Production Environment Configuration
# Copy this to .env.production and fill in your values

# Server Configuration
NODE_ENV=production
PORT=3001

# Security
# Generate a strong secret: openssl rand -base64 32
SESSION_SECRET=your-strong-session-secret-here

# CORS Configuration
# Add your production frontend URL(s)
FRONTEND_URL=https://your-app-domain.com
# For multiple origins: FRONTEND_URL=https://domain1.com,https://domain2.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # Max requests per window

# Apple API Configuration
# Store these securely - consider using environment variables from your hosting provider
# NEVER commit actual values to git

# Optional: If you want to store credentials server-side (more secure)
# Otherwise, credentials will be sent from frontend each time
# APPLE_APP_ID=your-app-id
# APPLE_ISSUER_ID=your-issuer-id-uuid
# APPLE_KEY_ID=your-key-id
# APPLE_PRIVATE_KEY_PATH=/secure/path/to/AuthKey_XXXXXXXXXX.p8

# Logging
LOG_LEVEL=info
LOG_FILE=/var/log/review-dashboard/app.log

# SSL/TLS (if terminating SSL at app level)
# SSL_KEY_PATH=/path/to/private.key
# SSL_CERT_PATH=/path/to/certificate.crt

# Database (for caching reviews - optional)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Redis (for session/cache - optional)
# REDIS_URL=redis://user:password@host:6379

# Monitoring (optional)
# SENTRY_DSN=https://your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your-new-relic-key