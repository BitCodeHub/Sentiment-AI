# Server Configuration
PORT=3001

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Apple App Store API Configuration
# These are used server-side only, never exposed to frontend

# Optional: Default credentials for testing
# WARNING: Never commit real credentials
# APPLE_APP_ID=your-app-id
# APPLE_ISSUER_ID=your-issuer-id
# APPLE_KEY_ID=your-key-id

# Rate limiting (optional)
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # Max requests per window

# Reddit API Configuration
# Create an app at: https://www.reddit.com/prefs/apps
# Choose "script" type for backend applications
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret